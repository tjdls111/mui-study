import { createTheme } from "@mui/material";

const theme = createTheme({
  light: {
    fontColor: "black",
    bgColor: "white",
  },
  dark: {
    fontColor: "white",
    bgColor: "black",
  },
});

export default theme;
